services:
  linka:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - linka-data:/data
    environment:
      - DB_PATH=/data/linka.db
      - PORT=3000
    restart: unless-stopped

volumes:
  linka-data:
